securitySchemes:

  accessCookie:
    type: apiKey
    in: cookie
    name: jwt

  refreshCookie:
    type: apiKey
    in: cookie
    name: rft

  csrfCookie:
    type: apiKey
    in: cookie
    name: csrf