get:
  summary: Listing des personnalités politiques
  description: Aucune authentification requise.
  tags: [ Politicians ]
  operationId: listPoliticians
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              type: object
              properties:
                id: { type: string, format: uuid }
                fullName: { type: string }
